<%= form_for(@customer) do |f| %>
  <% if @customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customer.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @customer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <h4 class="text-uppercase">Update <%= @customer.email %> </h4>
 <div class="form-group">
   <%= f.text_field :firstname, class:"form-control", placeholder:"Firstname" %>
 </div>
 <div class="form-group">
   <%= f.text_field :lastname, class:"form-control", placeholder:"Lastname" %>
 </div>
 <div class="form-group">
   <%= f.email_field :email, class:"form-control", placeholder:"Email" %>
 </div>
 <div class="form-group">
   <%= f.telephone_field :phone1, class:"form-control", placeholder:"Phone" %>
 </div>
  <div class="actions">
    <%= f.submit "Update", class: "btn btn-primary btn-lg" %>
  </div>
<% end %>

